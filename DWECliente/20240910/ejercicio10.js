let coche = {
  marca: 'Toyota',
  modelo: 'CV-200',
  year: 2000,
}

const respuesta = () => {
  return `El coche es un ${coche.marca}, modelo ${coche.modelo} del año ${coche.year}`
}

alert(respuesta())
